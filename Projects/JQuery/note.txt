06-JQUERY

Things i learnt
. CDN - Content Delivery Network
. Some Bootstrap temp.
. Leanrt $ is used for my selections. No need for querySelectors again sef.


. Leant this methods: (
    css() - To add CSS styles, takes 2 params, 1 the style, 2 the value.
    , addClass() - to add Class
    , removeClass() - to remove class
    , hasClass() - returns True if class exists and false if otherwise.
     text() - changes the content of element.
     html() - chnages the content of element but allows html code too.
     attr() - 1 attribute specified as the param, prints the value of attr, two param updates it to the second value.
    )


.Learnt this Event Listeners:
(
    .click(function() {
        //Statements
    })
   .keypress(function() {
        //Statements
    })

    .on("EventName", function() {

    })
    this on() works excatly like addEventListener() func.
)



. Learnt functions for adding/Removing Elements:
(
    .before() - add elemwnt before the selected one.
    .after() - add elemwnt after the selected one.
    .prepend() - add element inside the selected element immediately after the opening tag.
    .append() - add element inside the selected element before the closing tag.
)

. Learnt some animation fundtions:
(
    show(), fadeIn(), slideDown(), 
    hide(), fadeOut(), slideUp(), 
    toggle(), fadeToggle(), slideToggle(), 
    animate("css proprty":"value") - The css property can only be one accepts number
)